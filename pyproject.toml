[project]
name = "activity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "instructor>=1.7.0",
    "jupytext>=1.16.6",
    "python-dotenv>=1.0.1",
]
